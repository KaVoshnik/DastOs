# MyOS v0.7 - Современная учебная операционная система

## 🚀 Обзор проекта

MyOS v0.7 - это полнофункциональная 32-битная операционная система с расширенными возможностями, включая полную поддержку ELF-загрузчика, многозадачность, виртуальную память и пользовательский режим. Система разработана как образовательный проект для изучения принципов работы операционных систем.

## ✨ Ключевые возможности

### 🔧 ELF-загрузчик (полная реализация)
- **Полная поддержка ELF32** - парсинг заголовков, программных сегментов, секций
- **Валидация формата** - проверка магических чисел, архитектуры (i386), типов файлов
- **Умная загрузка** - размещение сегментов по виртуальным адресам с учетом прав доступа
- **BSS-инициализация** - автоматическое обнуление неинициализированных данных
- **Интеграция с планировщиком** - создание пользовательских задач из ELF-программ
- **Файловое исполнение** - загрузка и запуск программ из файловой системы

### 🔄 Многозадачность и планировщик
- **Round-Robin планировщик** - справедливое распределение процессорного времени (10ms слайсы)
- **Полное переключение контекста** - сохранение/восстановление всех регистров и состояний
- **Пользовательский режим (Ring 3)** - изоляция процессов с защитой памяти
- **Системные вызовы** - INT 0x80 интерфейс с поддержкой до 5 аргументов
- **Приоритеты задач** - 256-уровневая система приоритетов
- **Управление жизненным циклом** - создание, завершение, ожидание процессов

### 💾 Управление памятью
- **Динамический аллокатор** - управление кучей с фрагментацией и объединением блоков
- **Виртуальная память** - полная реализация paging с 4KB страницами
- **Page Directory и Page Tables** - двухуровневая система трансляции адресов
- **Защита памяти** - разграничение между режимами ядра и пользователя
- **Page Fault обработка** - обнаружение и обработка нарушений доступа к памяти
- **Мониторинг использования** - статистика свободной/занятой памяти

### 📁 Файловая система
- **В памяти (In-Memory)** - быстрая файловая система с inode структурой
- **Полные операции** - создание, чтение, запись, удаление файлов
- **Метаданные** - поддержка времени создания/модификации файлов
- **Интеграция с ELF** - прямая загрузка исполняемых файлов из ФС
- **Битовые карты** - эффективное управление свободными блоками и inodes

### 🎯 Системные вызовы (INT 0x80)
- **SYS_EXIT** (0) - корректное завершение процесса
- **SYS_WRITE** (1) - вывод данных в терминал
- **SYS_READ** (2) - чтение данных (базовая реализация)
- **SYS_GETPID** (8) - получение ID текущего процесса
- **SYS_YIELD** (9) - добровольная передача управления планировщику
- **SYS_SLEEP** (10) - приостановка выполнения на заданное время

### ⌨️ Расширенная поддержка клавиатуры
- **Сканкоды Set 1** - полная таблица обычных и расширенных клавиш
- **Модификаторы** - Shift, Ctrl, Alt (левые/правые), Caps Lock, Num Lock
- **Специальные клавиши** - F1-F12, стрелки, Home/End/PgUp/PgDn, Insert/Delete
- **Светодиоды** - управление Caps Lock, Num Lock, Scroll Lock
- **ASCII конверсия** - правильная обработка регистра и специальных символов
- **Событийная архитектура** - callback система для обработки нажатий

### 🖥️ VGA текстовый режим
- **80x25 символов** - стандартное разрешение с цветной поддержкой
- **VGA курсор** - аппаратный мигающий курсор с настраиваемым размером
- **16 цветов** - полная палитра для текста и фона
- **Прокрутка** - автоматическая прокрутка при переполнении экрана
- **Форматированный вывод** - поддержка чисел, строк, специальных символов

### 🔧 Обработка прерываний
- **IDT (Interrupt Descriptor Table)** - 256 записей для всех векторов
- **Исключения процессора** - обработка Page Fault, General Protection и других
- **IRQ обработчики** - таймер (IRQ0), клавиатура (IRQ1)
- **PIC настройка** - программирование контроллера прерываний
- **Системные вызовы** - INT 0x80 с полным сохранением контекста

## 🏗️ Архитектура системы

### Структура проекта
```
MyOS v0.7/
├── src/
│   ├── boot/
│   │   └── boot.asm              # Multiboot загрузчик
│   ├── kernel/
│   │   ├── kernel.c              # Основное ядро (2000+ строк)
│   │   ├── keyboard.c            # Модуль клавиатуры
│   │   ├── interrupts.asm        # Обработчики прерываний
│   │   ├── context.asm           # Переключение контекста
│   │   ├── syscalls.asm          # Системные вызовы
│   │   └── usermode.asm          # Пользовательский режим
│   ├── include/
│   │   ├── elf.h                 # ELF структуры и константы
│   │   ├── keyboard.h            # API клавиатуры
│   │   └── types.h               # Базовые типы данных
│   └── linker.ld                 # Скрипт компоновщика
├── build/                        # Директория сборки
├── isodir/                       # ISO образ
├── Makefile                      # Система сборки
└── README.md                     # Документация
```

### Модули и компоненты

#### 🎯 ELF-загрузчик
```c
// Основные функции
int elf_validate(uint8_t* data, uint32_t size);           // Валидация ELF
int elf_parse(elf_loader_t* loader, uint8_t* data, uint32_t size); // Парсинг
uint32_t elf_load_program(elf_loader_t* loader);         // Загрузка в память
task_t* create_elf_task(const char* name, uint8_t* elf_data, 
                       uint32_t elf_size, uint32_t priority); // Создание задачи
```

#### 🔄 Планировщик задач
```c
// Структура задачи
typedef struct task {
    uint32_t id;                    // Уникальный ID
    char name[32];                  // Имя задачи
    uint32_t state;                 // Состояние (RUNNING/READY/BLOCKED/DEAD)
    uint32_t priority;              // Приоритет (0-255)
    registers_t regs;               // Сохраненные регистры
    uint32_t* stack;                // Указатель на стек
    uint32_t stack_size;            // Размер стека
    elf_loader_t* elf_loader;       // ELF-загрузчик
    struct task* next;              // Следующая задача в списке
} task_t;
```

#### 💾 Виртуальная память
```c
// Константы paging
#define PAGE_SIZE           4096    // 4KB страницы
#define PAGE_ENTRIES        1024    // 1024 записи в таблице
#define PAGE_PRESENT        0x001   // Страница присутствует
#define PAGE_WRITABLE       0x002   // Доступна для записи
#define PAGE_USER           0x004   // Доступна пользователю
```

## 💻 Интерактивная командная оболочка

### Системные команды
- **help** - список всех доступных команд с описанием
- **about** - подробная информация о системе и версии
- **clear** - очистка экрана терминала
- **memory** - статистика использования памяти (общая/свободная/занятая)
- **memtest** - тестирование аллокатора памяти
- **tasks** - список всех активных задач с состояниями
- **schedule** - принудительный запуск планировщика
- **keyboard** - состояние клавиатуры и активных модификаторов

### ELF и файловые команды
- **testelf** - тест встроенной ELF-программы "Hello World"
- **run \<filename\>** - загрузка и выполнение ELF-программы из файла
- **ls** - список всех файлов в файловой системе
- **touch \<filename\>** - создание нового пустого файла
- **cat \<filename\>** - вывод содержимого файла
- **rm \<filename\>** - удаление файла из системы
- **echo \<text\> > \<file\>** - запись текста в файл

### Системные операции
- **reboot** - перезагрузка системы (программная)
- **poweroff** - завершение работы системы

## 🛠️ Сборка и запуск

### Требования к среде разработки
- **NASM** - Netwide Assembler для .asm файлов
- **GCC** - компилятор с поддержкой 32-битных целей
- **Binutils** - GNU LD линковщик и утилиты
- **GRUB2** - для создания загрузочного ISO образа
- **QEMU** - эмулятор для тестирования системы

### Команды сборки
```bash
make all           # Полная сборка ISO образа
make kernel        # Сборка только ядра (.bin файл)
make run-iso       # Запуск ISO в QEMU
make run-bin       # Прямой запуск ядра в QEMU
make run-vnc       # Запуск с VNC доступом
make clean         # Очистка всех собранных файлов
make rebuild       # Полная пересборка
make info          # Информация о системе сборки
make tree          # Структура проекта
```

### Быстрый старт
```bash
# Сборка и запуск системы
make all && make run-iso

# Или сборка только ядра и запуск
make kernel && make run-bin
```

## 🎮 Примеры использования

### Тестирование ELF-загрузчика
```bash
MyOS v0.7> testelf
Loading built-in ELF test program...
[ELF] Validating ELF file... OK
[ELF] Parsing headers... OK  
[ELF] Loading program segments... OK
[ELF] Creating user task... OK
[ELF] Task created: hello_world (ID: 2)
Hello from ELF program!
Program completed successfully.
```

### Работа с файлами
```bash
MyOS v0.7> touch myprogram.elf
File 'myprogram.elf' created successfully.

MyOS v0.7> ls
Files in system:
- myprogram.elf (0 bytes)

MyOS v0.7> echo "Hello from MyOS!" > greeting.txt
MyOS v0.7> cat greeting.txt  
Hello from MyOS!
```

### Мониторинг системы
```bash
MyOS v0.7> memory
Memory Usage:
  Total: 1048576 bytes
  Free:  891234 bytes  
  Used:  157342 bytes
  Usage: 15%

MyOS v0.7> tasks
Active Tasks:
1. kernel_idle (ID: 1, Priority: 255, State: READY)
2. demo_task1 (ID: 2, Priority: 10, State: RUNNING)
3. demo_task2 (ID: 3, Priority: 20, State: READY)
```

## 🔧 Техническая реализация

### Особенности ELF-загрузчика
- Валидация всех критических полей ELF заголовка
- Поддержка только исполняемых файлов (ET_EXEC) архитектуры i386
- Проверка выравнивания и размеров сегментов
- Автоматическое создание пользовательских задач с корректными правами

### Безопасность и изоляция
- Полное разделение Ring 0 (ядро) и Ring 3 (пользователь)
- Валидация всех пользовательских указателей
- Проверка границ при работе с памятью
- Защита критических структур данных ядра

### Производительность
- Эффективное переключение контекста (< 1μs)
- Минимальные накладные расходы планировщика
- Оптимизированный аллокатор памяти с объединением блоков
- Быстрая обработка прерываний

## 🎓 Образовательная ценность

MyOS v0.7 демонстрирует:
- **Загрузку ОС** - от Multiboot до запуска шелла
- **Управление памятью** - физическая и виртуальная память
- **Планирование задач** - многозадачность и переключение контекста
- **Системные вызовы** - интерфейс между пользователем и ядром
- **Обработку прерываний** - взаимодействие с аппаратурой
- **Файловые системы** - хранение и управление данными
- **ELF формат** - загрузка исполняемых файлов

## 📈 История версий

### v0.7 (Текущая версия)
- ✅ **Полный ELF-загрузчик** - валидация, парсинг, загрузка, выполнение
- ✅ **Расширенная многозадачность** - пользовательский режим, системные вызовы
- ✅ **Улучшенная файловая система** - метаданные, эффективное управление
- ✅ **Модульная архитектура клавиатуры** - событийная система, расширенные клавиши
- ✅ **Виртуальная память** - полный paging, защита памяти
- ✅ **Интерактивная оболочка** - 15+ команд, ELF интеграция

### Планы развития
- **Динамические библиотеки** - поддержка .so файлов
- **Сетевые возможности** - TCP/IP стек
- **Графический режим** - VESA/VBE поддержка  
- **Драйверы устройств** - модульная система драйверов
- **SMP поддержка** - многопроцессорность
- **Журналируемая ФС** - надежное хранение данных

## 👨‍💻 Для разработчиков

### Добавление новых системных вызовов
```c
// В kernel.c
#define SYS_MYNEWCALL 11

int handle_syscall(int syscall_num, int arg0, int arg1, int arg2, int arg3, int arg4) {
    switch(syscall_num) {
        case SYS_MYNEWCALL:
            return my_new_function(arg0, arg1);
        // ...
    }
}
```

### Создание новых команд шелла
```c
// В kernel.c
void command_mycommand(void) {
    terminal_writestring("My custom command executed!\n");
}

// В execute_command()
if (strcmp(command, "mycommand") == 0) {
    command_mycommand();
}
```

---

**MyOS v0.7** - операционная система с полной реализацией ELF-загрузчика и расширенными возможностями для изучения принципов работы ОС.

*Разработано с ❤️ для изучения системного программирования*